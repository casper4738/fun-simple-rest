pluginManagement {
    repositories {
        // for non proxy
        gradlePluginPortal()

        // for proxy
        // maven { url "http://bcaartifactory.intra.bca.co.id:8080/artifactory/list/plugins.gradle.org/" }
    }
}
rootProject.name = 'FunSimpleRest'
